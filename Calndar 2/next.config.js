/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If this project is inside a monorepo, consider setting
  // `outputFileTracingRoot` to the workspace root to avoid bundling issues.
}

module.exports = nextConfig
