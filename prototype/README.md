# OliveBranch Prototype

This is a minimal Vite + React + TypeScript prototype for OliveBranch.

Quick start:

```bash
cd prototype
pnpm install
pnpm dev
```

This will start the dev server on `http://localhost:4173` by default.
